enum KzlySections { boca, event, wedding }

enum KzlyOrdersSections { all, done, pending, decline }

enum KzlyStatus { approved, pending, declined }

enum KzlyDays { sat, sun, mon, tue, wed, thu, fri }


enum KzlyOrdersSections2 { all, pending, confirmed, cancelled}
